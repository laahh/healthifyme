import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.projectaplikasihealth.app",
  appName: "Project Aplikasi Health",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
